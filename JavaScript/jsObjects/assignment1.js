// 1. Create a JSON object with keys: name, age,and city.
let info = {
  name: "Ayushi",
  age: "21",
  city: "Bhopal",
};

// 2. Create a JSON object that contains a name and an array of marks.
let student = {
  name: "Amit Sharma",
  marks: [85, 90, 78, 92, 88],
};

// 3. Create a JSON object with a nested object (student → name and age).

// 4. Write a JSON object that includes different data types (string, number, boolean, null).
let dataTypes = {
  "name": "Ayuuu",
  "age": 20,
  "isStudent": true,
  "phone": null
}